---
title: Série Temporal de Métricas
api: GET /v1/projects/{projectId}/metrics/timeseries
---

Retorna métricas diárias de visibilidade em IA para geração de gráficos. Cada linha corresponde a um dia do calendário dentro do período derivado de `timeframe`, ou dentro de `startDate` / `endDate` quando ambos são fornecidos. Ideal para linhas de tendência da Finseo no ChatGPT, Claude e Perplexity.

<ParamField path="projectId" type="string" required>
  Identificador do projeto.
</ParamField>

<ParamField query="timeframe" type="string" default="30d">
  Janela móvel quando `startDate` / `endDate` não são usados.
</ParamField>

<ParamField query="startDate" type="string">
  Data ISO `YYYY-MM-DD` (use com `endDate`).
</ParamField>

<ParamField query="endDate" type="string">
  Data ISO `YYYY-MM-DD` (use com `startDate`).
</ParamField>

<ParamField query="model" type="string">
  Filtre para um único modelo.
</ParamField>

<ParamField query="language" type="string">
  Filtro por idioma do Prompt.
</ParamField>

<ParamField query="tags" type="string">
  Array JSON ou Tags separadas por vírgula.
</ParamField>


<RequestExample>

```bash cURL
curl --request GET \
  --url 'https://api.finseo.ai/v1/projects/67a1c2d3e4f5061728394a5b/metrics/timeseries?timeframe=14d' \
  --header 'Authorization: Bearer sk_live_xxxxxxxx'
```

```python Python
import requests

r = requests.get(
    "https://api.finseo.ai/v1/projects/67a1c2d3e4f5061728394a5b/metrics/timeseries",
    headers={"Authorization": "Bearer sk_live_xxxxxxxx"},
    params={"timeframe": "14d", "model": "chatgpt"},
)
series = r.json()["data"]
print(series[-1]["mentions"], series[-1]["date"])
```

</RequestExample>

<ResponseExample>

```json
{
  "data": [
    {
      "date": "2026-03-15",
      "visible": 9,
      "notVisible": 3,
      "totalResults": 12,
      "mentions": 24,
      "position": 19.2,
      "sentiment": 0.74,
      "uniquePrompts": 12
    },
    {
      "date": "2026-03-16",
      "visible": 10,
      "notVisible": 2,
      "totalResults": 12,
      "mentions": 31,
      "position": 17.8,
      "sentiment": 0.79,
      "uniquePrompts": 12
    },
    {
      "date": "2026-03-17",
      "visible": 11,
      "notVisible": 1,
      "totalResults": 12,
      "mentions": 28,
      "position": 16.5,
      "sentiment": 0.81,
      "uniquePrompts": 12
    }
  ]
}
```

</ResponseExample>
